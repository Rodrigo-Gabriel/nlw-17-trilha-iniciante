let meta = {
    value: 'ler um livro todo por mes',
    address: 2,
    checked: false,
    log: (info) => {
        console.log(info);
    }
}

console.log(meta.value);

// function // arrow function
const criarMeta = () => {}

function criarMeta() {}